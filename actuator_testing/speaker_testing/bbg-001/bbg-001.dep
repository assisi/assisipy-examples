# Deployment specification file
# For each casu in the arena, we need to specifiy:
#     - the hostname of the target machine
#     - the controller and additional files to deploy

vibearena : 
    casu-001 : 
        hostname : bbg-001
        user : assisi
        prefix : deploy
        controller: controllers/vibration_pattern.py
    casu-002 : 
        hostname : bbg-001
        user : assisi
        prefix : deploy
        controller: controllers/vibration_pattern.py
    casu-003 : 
        hostname : bbg-001
        user : assisi
        prefix : deploy
        controller: controllers/vibration_pattern.py
    casu-004 : 
        hostname : bbg-001
        user : assisi
        prefix : deploy
        controller: controllers/vibration_pattern.py

